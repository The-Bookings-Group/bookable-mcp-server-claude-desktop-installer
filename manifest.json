{
  "dxt_version": "0.1",
  "name": "bookable-mcp-server-claude-desktop-installation",
  "display_name": "One-click connector to Bookable remote MCP",
  "version": "1.0.0",
  "description": "A simple, one-click solution to connect to Bookable's MCP server using Streamable HTTP connection.",
  "author": {
    "name": "Bookable",
    "email": "hello@bookabletech.com"
  },
  "homepage": "https://www.bookabletech.com/",
  "documentation": "https://docs.bookabletech.com/",
  "icon": "icon.svg",
  "server": {
    "type": "node",
    "entry_point": "npm",
    "mcp_config": {
      "command": "npm",
      "args": [
        "--prefix",
        "${__dirname}",
        "run",
        "start",
        "--silent"
      ],
      "env": {
        "BOOKABLE_MCP_SERVER": "${user_config.mcp_server_url}",
        "BOOKABLE_AUTH_SERVER": "${user_config.authorization_server_url}",
        "BOOKABLE_CLIENT_ID": "${user_config.client_id}",
        "BOOKABLE_CLIENT_SECRET": "${user_config.client_secret}"
      }
    }
  },
  "tools": [
    {
      "name": "get_venues",
      "description": "Get a paginated list of venues (restaurants, pubs, bars, etc.)."
    },
    {
      "name": "get_venue_by_id",
      "description": "Get a venue by its ID."
    },
    {
      "name": "check_venue_availability",
      "description": "Check the availability of a venue."
    },
    {
      "name": "create_venue_booking",
      "description": "Create a new venue booking."
    },
    {
      "name": "get_venue_bookings",
      "description": "Get a paginated list of venue bookings."
    },
    {
      "name": "get_venue_booking_by_id",
      "description": "Get an existing booking by its ID."
    },
    {
      "name": "update_venue_booking",
      "description": "Update an existing booking by its ID."
    },
    {
      "name": "cancel_venue_booking",
      "description": "Cancel an existing booking by its ID."
    }
  ],
  "keywords": [
    "booking",
    "venues",
    "restaurants",
    "pub",
    "bar",
    "rms"
  ],
  "license": "MIT",
  "user_config": {
    "mcp_server_url": {
      "type": "string",
      "title": "MCP Server URL",
      "description": "The URL of the remote MCP server.",
      "default": "http://localhost:8080/mcp"
    },
    "authorization_server_url": {
      "type": "string",
      "title": "Authorization Server URL",
      "description": "The URL of the OAuth2 authorization server.",
      "default": "https://bookabletech.uk.auth0.com"
    },
    "client_id": {
      "type": "string",
      "title": "Client ID",
      "description": "The Client ID for the OAuth2 client credentials flow.",
      "required": true
    },
    "client_secret": {
      "type": "string",
      "title": "Client Secret",
      "description": "The Client Secret for the OAuth2 client credentials flow.",
      "sensitive": true,
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=0.10.0",
    "platforms": ["darwin", "win32", "linux"],
    "runtimes": {
      "node": ">=16.0.0"
    }
  }
}
